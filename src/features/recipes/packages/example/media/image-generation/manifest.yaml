# Image Generation - Media Recipe
version: 1

id: image-generation
name: Image Generation
description: Generate images from text prompts
category: Media
icon: Image

model:
  category: image-generation
  capabilities:
    - vision
  defaultParams: {}

advanced:
  multiTurn: false

input:
  schema:
    $ref: ./schemas/input.yaml

output:
  node: gallery
  title: "Generated: {{prompt}}"
  collapsed: false
